services:
  app:
    build: .
    container_name: ipmanager_app
    ports:
      - "8080:80"
    environment:
      - DB_HOST=db
      - DB_NAME=ipmanage
      - DB_USER=ipmanager
      - DB_PASS=ipmanager_pass
      - DISCOVERY_AGGRESSIVE_MODE=1
      - ENABLE_NMAP_FALLBACK=1
      - OFFLINE_TTL_MINUTES=30
      - DOCKER_ENV=1
    depends_on:
      db:
        condition: service_healthy
    networks:
      - ipmanage_net

  db:
    image: mariadb:10.11
    container_name: ipmanager_db
    ports:
      - "3307:3306"
    environment:
      - MARIADB_ROOT_PASSWORD=root_password_secure
      - MARIADB_DATABASE=ipmanage
      - MARIADB_USER=ipmanager
      - MARIADB_PASSWORD=ipmanager_pass
    volumes:
      - db_data:/var/lib/mysql
      - ./sql/database.sql:/docker-entrypoint-initdb.d/database.sql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ipmanage_net

networks:
  ipmanage_net:
    driver: bridge

volumes:
  db_data:
